name: Build Windows

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    env:
      SDL_VERSION: 3.4.10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Apply transition-latched dual-trigger decoder
        shell: python
        run: |
          from pathlib import Path
          import re

          path = Path("WoWpadX/RawInputGamepad.cpp")
          text = path.read_text(encoding="utf-8-sig")

          state_pattern = re.compile(
              r"    struct TriggerCombinationState\n    \{.*?\n    \};\n\n    TriggerCombinationState triggerCombinationState;",
              re.DOTALL,
          )
          state_replacement = '''    struct TriggerCombinationState
    {
        bool digitalActive = false;
        int initialSide = 0;
        bool bothHeld = false;
        bool releaseArmed = false;
        int lastCombined = 128;
        std::chrono::steady_clock::time_point stableSince{};
    };

    TriggerCombinationState triggerCombinationState;'''
          if not state_pattern.search(text):
              raise SystemExit("TriggerCombinationState not found")
          text = state_pattern.sub(state_replacement, text, count=1)

          function = r'''    void ParseAllyAxes(
        const BYTE* report,
        ULONG reportLength,
        ControllerState& state)
    {
        if (reportLength < 11)
            return;

        state.axisKnown[SDL_GAMEPAD_AXIS_LEFTX] = true;
        state.axisKnown[SDL_GAMEPAD_AXIS_LEFTY] = true;
        state.axisKnown[SDL_GAMEPAD_AXIS_RIGHTX] = true;
        state.axisKnown[SDL_GAMEPAD_AXIS_RIGHTY] = true;
        state.axisKnown[SDL_GAMEPAD_AXIS_LEFT_TRIGGER] = true;
        state.axisKnown[SDL_GAMEPAD_AXIS_RIGHT_TRIGGER] = true;

        state.axes[SDL_GAMEPAD_AXIS_LEFTX] = NormalizeStick(report[2]);
        state.axes[SDL_GAMEPAD_AXIS_LEFTY] = NormalizeStick(report[4]);
        state.axes[SDL_GAMEPAD_AXIS_RIGHTX] = NormalizeStick(report[6]);
        state.axes[SDL_GAMEPAD_AXIS_RIGHTY] = NormalizeStick(report[8]);

        constexpr int triggerCenter = 128;
        constexpr int sideThreshold = 10;
        constexpr int stableNoise = 2;
        constexpr int releaseMovement = 12;
        constexpr auto releaseArmDelay = std::chrono::milliseconds(80);

        const int combinedTrigger = static_cast<int>(report[10]);
        const bool digitalTrigger = (report[9] & 0x80) != 0;
        const int side = combinedTrigger < triggerCenter - sideThreshold
            ? -1
            : combinedTrigger > triggerCenter + sideThreshold
                ? 1
                : 0;
        const auto now = std::chrono::steady_clock::now();

        if (!digitalTrigger) {
            triggerCombinationState = {};
        }
        else if (!triggerCombinationState.digitalActive) {
            triggerCombinationState.digitalActive = true;
            triggerCombinationState.initialSide = side;
            triggerCombinationState.lastCombined = combinedTrigger;
            triggerCombinationState.stableSince = now;
        }
        else if (!triggerCombinationState.bothHeld) {
            if (triggerCombinationState.initialSide == 0 && side != 0) {
                triggerCombinationState.initialSide = side;
            }
            else if (side != 0 &&
                     triggerCombinationState.initialSide != 0 &&
                     side != triggerCombinationState.initialSide) {
                // The digital trigger bit remained asserted while the combined
                // axis crossed to the opposite side. That can only happen when
                // the second trigger is pressed before the first is released.
                triggerCombinationState.bothHeld = true;
                triggerCombinationState.releaseArmed = false;
                triggerCombinationState.stableSince = now;
            }
            triggerCombinationState.lastCombined = combinedTrigger;
        }
        else {
            const int movement = std::abs(
                combinedTrigger - triggerCombinationState.lastCombined);

            if (!triggerCombinationState.releaseArmed) {
                if (movement <= stableNoise) {
                    if (now - triggerCombinationState.stableSince >=
                        releaseArmDelay) {
                        triggerCombinationState.releaseArmed = true;
                    }
                }
                else {
                    triggerCombinationState.stableSince = now;
                }
            }
            else if (movement >= releaseMovement) {
                // Once the chord has settled, a substantial axis movement means
                // one trigger was released. Return to the remaining trigger side.
                triggerCombinationState.bothHeld = false;
                triggerCombinationState.initialSide = side;
                triggerCombinationState.releaseArmed = false;
                triggerCombinationState.stableSince = now;
            }

            triggerCombinationState.lastCombined = combinedTrigger;
        }

        if (triggerCombinationState.bothHeld) {
            state.axes[SDL_GAMEPAD_AXIS_LEFT_TRIGGER] = 32767;
            state.axes[SDL_GAMEPAD_AXIS_RIGHT_TRIGGER] = 32767;
            return;
        }

        const int leftMagnitude = combinedTrigger < triggerCenter
            ? triggerCenter - combinedTrigger
            : 0;
        const int rightMagnitude = combinedTrigger > triggerCenter
            ? combinedTrigger - triggerCenter
            : 0;

        state.axes[SDL_GAMEPAD_AXIS_LEFT_TRIGGER] =
            NormalizeTriggerMagnitude(leftMagnitude, triggerCenter);
        state.axes[SDL_GAMEPAD_AXIS_RIGHT_TRIGGER] =
            NormalizeTriggerMagnitude(rightMagnitude, 127);
    }

'''
          function_pattern = re.compile(
              r"    void ParseAllyAxes\(.*?\n    \}\n\n    bool LoadDevice",
              re.DOTALL,
          )
          if not function_pattern.search(text):
              raise SystemExit("ParseAllyAxes not found")
          text = function_pattern.sub(function + "    bool LoadDevice", text, count=1)
          path.write_text(text, encoding="utf-8")

      - name: Set up MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.9.1
          arch: win64_msvc2022_64
          cache: true

      - name: Install SDL
        shell: pwsh
        run: |
          $archive = Join-Path $env:RUNNER_TEMP "SDL3-devel-$env:SDL_VERSION-VC.zip"
          $extractRoot = Join-Path $env:RUNNER_TEMP "sdl"
          $url = "https://github.com/libsdl-org/SDL/releases/download/release-$env:SDL_VERSION/SDL3-devel-$env:SDL_VERSION-VC.zip"

          Invoke-WebRequest -Uri $url -OutFile $archive
          Expand-Archive -Path $archive -DestinationPath $extractRoot -Force

          $sdlRoot = Join-Path $extractRoot "SDL3-$env:SDL_VERSION"
          if (-not (Test-Path (Join-Path $sdlRoot "include\SDL3\SDL.h"))) {
            throw "SDL headers were not found at $sdlRoot"
          }

          "SDL3_ROOT=$sdlRoot" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Configure WoWpadX
        shell: pwsh
        run: |
          cmake `
            -S (Join-Path $env:GITHUB_WORKSPACE "WoWpadX") `
            -B (Join-Path $env:GITHUB_WORKSPACE "build") `
            -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            "-DCMAKE_PREFIX_PATH=$env:QT_ROOT_DIR" `
            "-DSDL3_ROOT=$env:SDL3_ROOT"

          if ($LASTEXITCODE -ne 0) {
            throw "CMake configuration failed with exit code $LASTEXITCODE"
          }

      - name: Build WoWpadX
        shell: pwsh
        run: |
          cmake --build (Join-Path $env:GITHUB_WORKSPACE "build") --parallel

          if ($LASTEXITCODE -ne 0) {
            throw "CMake build failed with exit code $LASTEXITCODE"
          }

      - name: Stage portable build
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Path (Join-Path $env:GITHUB_WORKSPACE "build") -Recurse -Filter WoWpadX.exe |
            Select-Object -First 1

          if (-not $exe) {
            throw "WoWpadX.exe was not produced"
          }

          $stage = Join-Path $env:GITHUB_WORKSPACE "artifact"
          New-Item -ItemType Directory -Path $stage -Force | Out-Null
          Copy-Item -Path $exe.FullName -Destination (Join-Path $stage "WoWpadX.exe") -Force

          $sdlDll = Join-Path $env:SDL3_ROOT "lib\x64\SDL3.dll"
          Copy-Item -Path $sdlDll -Destination (Join-Path $stage "SDL3.dll") -Force

          $deployQt = Join-Path $env:QT_ROOT_DIR "bin\windeployqt.exe"
          & $deployQt --release --qmldir (Join-Path $env:GITHUB_WORKSPACE "WoWpadX\QML") (Join-Path $stage "WoWpadX.exe")

          if ($LASTEXITCODE -ne 0) {
            throw "windeployqt failed with exit code $LASTEXITCODE"
          }

      - name: Upload portable build
        uses: actions/upload-artifact@v4
        with:
          name: WoWpadX-xbox-dual-trigger-transition-test-windows-x64
          path: artifact
          if-no-files-found: error
